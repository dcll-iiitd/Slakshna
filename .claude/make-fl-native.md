# This file provides guidance to Claude Code (claude.ai/code) to remove the blockchain terminologies and make it native to peer-to-peer federated learning.

- Reframe the terms and notions related to blockchain and replace them with equivalent notions from native federated learning.

# Avoid

- Each node has its local ledger to save the .b64 files corresponding to the update deltas. This is used to be shared with peer nodes. Do not remove this ledger formation. Preferably try renaming this.